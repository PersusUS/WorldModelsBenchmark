[pytest]
testpaths = tests
pythonpath = . tests
markers =
    integration: constructs real simulators (MiniGrid / MuJoCo / dm_control); slower
    slow: runs a short training loop; seconds rather than milliseconds
filterwarnings =
    ignore::DeprecationWarning
